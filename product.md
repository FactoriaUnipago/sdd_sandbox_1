# Product Overview

> ⚠️ This is a placeholder. Generate the full product.md by running one of:
> - `project-scanner` (for existing projects — scans codebase and fills automatically)
> - `project-initializer` (for new projects — interactive setup)
>
> Do NOT fill this manually — let the agent generate it from your actual project files.
