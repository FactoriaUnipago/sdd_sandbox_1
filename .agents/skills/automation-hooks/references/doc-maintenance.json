{
  "version": "v1",
  "hooks": [
    {
      "name": "doc-maintenance-reminder",
      "trigger": "PostFileSave",
      "matcher": "(routes|api|controllers)/.*\\.(ts|js|py)$",
      "action": {
        "type": "agent",
        "prompt": "API/route file modified. Check if docs/architecture/api-contract.md needs updating. If the endpoint signature, parameters, or response changed, update the API docs."
      }
    }
  ]
}
