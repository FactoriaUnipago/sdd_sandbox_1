{
  "version": "v1",
  "hooks": [
    {
      "name": "no-hardcoded-secrets",
      "trigger": "PostFileSave",
      "matcher": "\\.(ts|js|py|json|env)$",
      "action": {
        "type": "agent",
        "prompt": "Check this file for hardcoded secrets (password, secret, api_key, token assignments with literal string values). If found: report ERROR and suggest using AWS Secrets Manager or environment variables."
      }
    },
    {
      "name": "test-reminder",
      "trigger": "PostFileCreate",
      "matcher": "(src|app)/.*\\.(ts|py|tsx|jsx)$",
      "action": {
        "type": "agent",
        "prompt": "New source file created. Remind: ensure unit tests are added for this file."
      }
    }
  ]
}
